from datetime import date


today = date.today()

class Streak:
    currentStreak = 0
    lastStreak = 0
    def checkStreak(currentStreak, lastStreak):
        if last date variable > 24 hours
        strea == 0
    else streak += 1


    def captureLastStreak():
        if picturetaken:
            lastPictureDate = date.today()
